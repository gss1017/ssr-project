import React from 'react';
import Header from '../../components/Header';

const Login = function () {
    return (
        <div>
            <Header/>
            <div>Login</div>
        </div>
    );
};

export default Login;
